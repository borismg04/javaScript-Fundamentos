// Condicionales IF

const disponible = 4000;
const retirar = 100;

if (disponible>=retirar){
  //Se cumple la condicion y se ejecuta el bloque de codigo
    console.log("Retiro exitoso");
} else {
  //No se cumple la condicion y no se ejecuta el bloque de codigo
    console.log("No hay suficiente dinero");
}

/**
 * > mayor que
 * < menor que
 * >= mayor o igual que
 * <= menor o igual que
 * == igual que
 * != diferente que
 * === identico que
 * !== no identico que
 * && and
 * || or
 * ! not
 */
